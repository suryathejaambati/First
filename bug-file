sudo yum install git
git init
git status
git add
git commit
git push origin master
git remote -v
git pull origin master
git remote -v
git remote
git remote add origin " github url"
git branch
gut branch -l
git branch -d "branch name"
git branch -a " branch name"
git checkout "branch name"
git branch --list
git branch "new branch name"
git push origin "new branch name"
git checkout "old branch name"
git banch -l
git merge "new branch name"
fork
git fetch
git tag v0.1
git tag -l
git tag v0.2 "commit id"
git tag -l
git tag -d v0.1
git push origin v0.1
git push origin --tags
git push origin :refs/tags/v0.1 
git show v2.3
GIT UNDOING:
git checkout "filename" : Working Area
git reset --soft HEAD~1 : Staging area
git reset HEAD~1 (Mixed)
git reset --hard HEAD~1
git revert " commit id" : History Area
git log --pretty=oneline
git commit --amend -m "Change the commit msg"
git merge --squash "branch name " : It won't provide any history of the data from the feature branch"
git rebase "branch name" : It should provide commit history from the branch"
git cherry-pic "commit id " : Take a latest commit msg id from feature and merge to monther branch( I mean provice the latest commit data only)
git stash : This is for hold the data on working environmnet. Once execute won't commit the changes in index area
git stash pop: to revert the our changes
git commit --amend -m " msg " : This command for revert the latest commit msg
git rebase -i HEAD~5  : We can modify the last 05 commit msgs using this command
git rebase -i "CommitID"^ : Change the msg for particular commit id only
.gitignore: This folder for store the extension file. once you update the any extention it won't push the changes to remote repository


________________________________________________________________________________________________________________________________


Jenkins

1. Build Automation 
 three types
    Poll SCM
    Build Periodically
    WebHooks





